public static void rotate(int arr[], int n, int d){
//enter your code here
       int k=1;
       while(k<=d){
           int last=arr[0];
           for(int i=0;i<n-1;i++){
                arr[i]=arr[i+1];
           }
           arr[n-1]=last;
           k++;
       }
        
}
